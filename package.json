{
    "private": true,
    "scripts": {
        "install": "npm --prefix ./vue install",
        "serve": "npm run --prefix ./vue serve",
        "build": "npm run --prefix ./vue build",
        "lint": "npm run --prefix ./vue lint",
        "dev" : "php artisan serve --host 0.0.0.0 --port 1994 | npm run serve"
    }
}
